# AetherMind · Market · оферта № 88/2026-AI

Маркет — мобильный каталог фиксированных категорий участия. Пользователь выбирает тариф, а не конкретный физический GPU, режим производительности или вычислительную задачу. Оборудование и B2B-нагрузку назначает Оператор после подтверждённого пополнения и заказа тарифа.

## Действующие параметры интерфейса

| Категория | Депозит | Epoch | Базовая ставка | Compound Boost | Расчёт за Epoch | Доступность |
|---|---:|---:|---:|---:|---:|---|
| Node Alpha | 50 USDT | 30 дней | 1,5% / сутки | 1,8% | 45% / 22,50 USDT | Выбор доступен |
| Cluster Beta | 300 USDT | 60 дней | 2,5% / сутки | 3% | 150% / 450 USDT | Выбор доступен |
| Enterprise POD | 1 200 USDT | 90 дней | 3,5% / сутки | 4,2% | 315% / 3 780 USDT | Выбор доступен |
| Quantum Array | 5 000 USDT | 180 дней | В разработке | В разработке | Не опубликован | Заблокирован, Q4 2026 |

Источник параметров — оригинальный PDF в `frontend/public/documents/public-offer-aethermind.pdf`. UI выводит расчётные значения, но не создаёт начисление.

## UX

- Две компактные карточки в ряд на обычном телефоне; одна колонка на экранах уже 346 px.
- В карточке остаются только депозит, Epoch, базовая ставка, расчёт за срок и Compound Boost.
- Технические сведения и роль Оператора вынесены в детальное окно.
- Категорию можно отфильтровать и отсортировать; Quantum визуально помечен как будущий и не выбирается.
- Сохранённая категория подсвечивается и отображается в «Профиле».

## API и данные

- `GET /api/v1/market` и `GET /api/v1/market/:id` — параметры каталога.
- `POST /api/profile/tariff` — идемпотентный выбор доступной категории после принятия документов; денег не списывает.
- `GET /api/v1/market/leases` — только собственные записи пользователя.
- `POST /api/v1/market/buy` — закрыт на сервере и возвращает `PAYMENTS_DISABLED`.

Баланс хранится в едином `LedgerEntry` в микро-USDT. Интерфейс основного баланса показывает целые USDT, не меняя точность серверного учёта. В БД сохраняются уже существующие аккаунты, обращения, заявки, ledger и аренды.

Подготовленный `reserveLease` не подключён к HTTP. Его будущий вызов обязан выполняться в одной PostgreSQL-транзакции с блокировкой пользователя и каталога, idempotency key, проверкой баланса, snapshot условий, записью ledger и audit. Одной переменной окружения недостаточно, чтобы открыть покупки.

## Партнёрская сеть

Партнёрская ссылка работает через `/start r_<telegram_id>`. Самореферал запрещён. В интерфейсе показываются анонимные фактические стадии приглашённого: регистрация, принятие оферты, выбор тарифа и активный Epoch. Оферта не содержит денежного реферального вознаграждения, поэтому `REFERRAL_BONUS` не создаётся и потенциальная прибыль не отображается.

## Следующий технический этап

До включения финансов необходимо реализовать и проверить: платёжный provider adapter, подписанные webhooks и сверку транзакций; версионируемый заказ тарифа; provisioning/outbox; идемпотентный расчёт Epoch; правила Compound Boost; offsite backup, мониторинг и процедуры возврата. До этого `paymentsEnabled`, `purchasesEnabled`, `accrualEnabled` и `compoundEnabled` остаются `false`.
